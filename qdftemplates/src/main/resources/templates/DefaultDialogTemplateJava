package ${packageName};

import android.os.Bundle;
import ${parentClassPath};
import ${applicationPackage}.R;
import ${applicationPackage}.databinding.Dialog${noTypeClassName}Binding;

${classHeader}
public class ${noTypeClassName}Dialog extends ${parentClassName} {

    private Dialog${noTypeClassName}Binding viewBinding = null;

    public ${noTypeClassName}Dialog(@NonNull Context context) {
        super(context);
    }

    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        viewBinding = Dialog${noTypeClassName}Binding.inflate(getLayoutInflater());
        setContentView(viewBinding.getRoot());
        initViews();
        
    }

    private void initViews() {

    }
} 